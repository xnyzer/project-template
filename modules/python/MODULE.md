# Module: python

Python with **uv** as the single tool (env, lock, build) — senior defaults: **src layout**
with `py.typed`, **ruff** as formatter + linter (incl. import sorting), **pyright strict**,
**pytest** under `tests/`. Python version = current stable via mise; `requires-python`
enforces the floor. Build backend: `uv_build`.

`{{CODEQL_LANGUAGES}}` → `python`

## Parts

| Part | Notes |
|------|-------|
| `justfile` | replaces core justfile; `check` = ruff format-check + ruff lint + pyright + pytest |
| `mise.part.toml` | python (current stable major.minor) + uv |
| `gitignore.part` | .venv, caches, dist |
| `CODING-STANDARDS.part.md` | Python rules for §13 |
| `files/` | pyproject.toml, src/ package skeleton, tests/ |

## File policies (files/)

| File | Policy |
|------|--------|
| `pyproject.toml` | seed for `[project]` (evolves with the project); the `[tool.*]` sections are managed conventions |
| `src/{{PROJECT_NAME_SNAKE}}/__init__.py`, `py.typed` | seed (sample code — replace freely) |
| `tests/test_smoke.py` | seed |

## Instantiation notes

- The directory `files/src/{{PROJECT_NAME_SNAKE}}/` is renamed at instantiation
  (kebab-case project name → snake_case package name); `uv_build` expects exactly that
  module under `src/`.
- `[project].license` expects a valid SPDX expression — when the license decision is
  "TBD", `/new-project` removes the field instead of writing an invalid value.
- `uv.lock` is generated by `just setup` (`uv sync`) and committed; Renovate keeps it fresh.
- `target-version` for ruff is derived automatically from `requires-python`.
