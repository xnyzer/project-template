# Toolchain pins — fuzzy majors here, exact versions in mise.lock (generated by
# `mise install`, committed). The stack module adds its runtimes to [tools].
[tools]
just = "1"
lefthook = "2"
gitleaks = "8"

[settings]
lockfile = true
